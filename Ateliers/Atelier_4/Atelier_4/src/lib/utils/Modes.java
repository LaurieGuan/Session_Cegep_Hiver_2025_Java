package lib.utils;

public class Modes {
    public enum descriptions {
        VERBOSE,
        BASIC,
        SILENT,
    }

    public enum getteurs {
        ATTRIBUT,
        ALEATOIRE,
    }

    public enum entites {
        FIGHTER,
        MAGE,
        CLERIC,
        MONSTER,
    }

    public enum status {
        FRIENDLY,
        ENNEMY,
    }
}
